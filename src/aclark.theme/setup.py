from setuptools import setup, find_packages

setup(
    name='aclark.theme',
    packages=find_packages(),
    namespace_packages=[
        'aclark'
        ]
    install_requires=[
        'setuptools'
        ]
    )
